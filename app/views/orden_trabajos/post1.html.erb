  <div class="d-flex justify-content-around">
  <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3">Buv</h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Observación</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>
    <%= if orden_trabajo.fecha_entrega == Date.today  %>
          <tr class="bg-dark font-weight-bold">
            <td><%= orden_trabajo.cliente %></td>
            <td><%= orden_trabajo.producto %></td>
            <td><%= truncate(orden_trabajo.observaciones, :length => 50)%></td>
            <td><%= orden_trabajo.fecha_entrega %></td>
            <td>
        <% end %>
        </tr>
        <% end %>
      </tbody>
    </table>
  </div>
    <div class="container">
      <table class="table table-striped">
        <h1 class="mt-3">Laminado</h1>
      <thead>
        <tr>
          <th>Cliente</th>
          <th>Producto</th>
          <th>Observación</th>
          <th>Fecha Entrega</th>
        </tr>
      </thead>
      <tbody>
    <% @orden_trabajos.each do |orden_trabajo| %>

          <tr>
            <td><%= orden_trabajo.cliente %></td>
            <td><%= orden_trabajo.producto %></td>
            <td><%= truncate(orden_trabajo.observaciones, :length => 50)%></td>
            <td> <%= orden_trabajo.fecha_entrega %></td>
          <% end %>
        </tr>
      </tbody>
    </table>
  </div>
</div>


<!--
//Using setTimeout to execute a function after 5 seconds.
setTimeout(function () {
   //Redirect with JavaScript
   window.location.href= 'http://localhost:3000/offset';
},  30000);
 -->
